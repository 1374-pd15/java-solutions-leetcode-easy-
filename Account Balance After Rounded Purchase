class Solution {
    public int accountBalanceAfterPurchase(int purchaseAmount) {
        int balance = 100;
        int roundedAmount = Math.round(purchaseAmount / 10.0f) * 10;
        int acc = balance - roundedAmount;
        return acc;
    }
}
